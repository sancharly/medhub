[project]
name = "medhub-dicom-viewer"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = []

[project.entry-points."medhub.modules"]
dicom-viewer = "dicom_viewer.manifest:manifest"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
